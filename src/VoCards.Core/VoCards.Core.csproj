<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>VoCards.Core</RootNamespace>
    <Description>VoCards domain: decks, cards, spaced-repetition scheduling, stats and gamification. No UI, no I/O.</Description>
  </PropertyGroup>

  <ItemGroup>
    <!-- The scheduling setters on Card are internal so that only CardReviewer writes
         them. The test project needs the same reach to arrange states directly. -->
    <InternalsVisibleTo Include="VoCards.Core.Tests" />
  </ItemGroup>

</Project>
